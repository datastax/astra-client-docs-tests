$schema: "../../../_base/meta.schema.json"
test:
  type: snapshot
execution: isolated
fixtures:
  base: basic-collection.js
snapshots:
  share: true
  sources:
    stderr::matches:
      regex: ^$
    collection::documents:
