$schema: "../../../_base/meta.schema.json"
test:
  type: snapshot
execution: isolated
fixtures:
  base: mutated-collection.js
snapshots:
  share: true
  sources:
    stderr::matches:
      regex: ^$
    collection::documents:
      projection: { "$vector": true }
