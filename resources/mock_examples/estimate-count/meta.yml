$schema: "../../_base/meta.schema.json"
fixtures:
  base: basic-collection.js
snapshots:
  share: true
  sources:
    stdout:
      matches: ^(?:\d+|\{"status":\{"count":\d+}})$ # Not deterministic so just any number will do
    stderr:
      matches: ^$
